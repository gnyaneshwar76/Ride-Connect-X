<?xml version="1.0" encoding="utf-8"?>
<!--
   android:allowBackup="false" stops cloud backup, but on Android 12+ it does
   NOT stop device-to-device transfer — that is governed here. Without this
   file wired up and these excludes, the Room database (emergency contacts,
   rides, notifications, service records), every DataStore preference file and
   the rider's avatar moved to a new phone on setup transfer.

   Nothing here needs transferring: the profile comes back from Firestore on
   sign-in, and the rest is a cache of the vehicle.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
